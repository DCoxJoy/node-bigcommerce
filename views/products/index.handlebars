<h1>My Datasheet</h1>
<div class="row">
    {{#each result}}
      <div class="col-12">
         <div>{{productid}}</div>
         <div>{{product_sku}}</div> 
         <div>{{product_name}}</div> 
         <div>{{zoom_image}}</div>
         <div>{{thumbnail_image}}</div>
         <div>{{standard_image}}</div>
         <div>{{tiny_image}}</div>
         <div>{{{truncate short_description 1400}}}</div>
           
      </div> 

      <div class="benefits">
            <span class="benefits-header">Benefits</span><br>
            <table>
              
            {{#each custom_fields}}
            {{this.value}}
            {{/each}}
            </table>
         </div>
         <div class="features">
            <span class="features-header">Features</span><br>
            <table>
             
            </table>
         </div>
         <div class="tech-specs-header">
         <p>
            Technical Specifications
            <br>
            <span class="underline"></span>
         </p>
      </div>


    {{else}}
      <p>No Datasheet Found</p>

    {{/each}}
    
</div>